def factor(num):
    factors = []
    for i in range(1, num+1):
        if num % i == 0:
            factors.append(i)
    return factors

def prime_composite(numbers):
    results = []
    for i in numbers:
        if len(factor(i)) < 3:
            results.append("PRIME")
        else:
            results.append("COMPOSITE")
    return results

print(prime_composite([1, 2, 3, 4, 5]))